@model IEnumerable<HelloWorld.Models.Workflow>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.EnterpriseApplication.ApplicationName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ServiceVersion.Version)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.EnterpriseApplication.ApplicationName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ServiceVersion.Version)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.WorkflowId }) |
            @Html.ActionLink("Details", "Details", new { id=item.WorkflowId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.WorkflowId })
        </td>
    </tr>
}

</table>
